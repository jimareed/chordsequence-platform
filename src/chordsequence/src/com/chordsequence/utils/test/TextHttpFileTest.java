package com.chordsequence.utils.test;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

import com.chordsequence.util.TextFileBufferString;
import com.chordsequence.util.TextHttpFile;

public class TextHttpFileTest {

	/*
	@Test
	public void test_basicHandler() {

		TextHttpFile file = new TextHttpFile();

		TextFileBufferString buffer = new TextFileBufferString("");
		
		file.setBuffer(buffer);

		assertTrue(file.load("http://chordsequence.com:8080/chordsequence/?view=opensong&songid=0&format=chordpro"));
		
		String contents = buffer.toString();
		
//		assertTrue(contents.contains("Chord Sequence"));
//		assertTrue(contents.contains("ChordPro format"));
	}*/
}
